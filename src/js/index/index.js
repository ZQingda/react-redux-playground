import React from 'react';
import ReactDOM from 'react-dom';
import index from '../index';

const title = 'BAD BOILER 2: REACT-REDUX BOOGALOO';

ReactDOM.render(
  <div>{title}</div>,
  document.getElementById('app')
);
